/**
 * @envy/react — thin React wrapper around the @envy/core runtime.
 *
 * The <Chart /> component lands once the core runtime (render/update/destroy)
 * is available. For now this re-exports the version marker.
 */
export const VERSION = '0.0.0';
